fd = socket(domain, type , protocol);
                    |
                    |
                    V

domine = AF_INET(IP), AF_INET6(IPv6), 
    AF_UNIX(local channel, similar to pipes),
    AF_ISO(ISO protocols), AF_NS(Xerox network system protocols);


type = SOCK_STREAM(virtual circuit service),
    SOCK_DGRAM(datagram service), SOCK_RAW(direct IP service);




socketadd_in -> built-in struct; // for adds